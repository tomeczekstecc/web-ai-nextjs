/**
 * Menu-specific icon resolution.
 * 
 * Re-exports from the central icon system for backward compatibility.
 * New code should import directly from `@/lib/icons`.
 */
export { resolveIcon } from "@/lib/icons";
